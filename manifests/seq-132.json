{
 "seq": 132,
 "mode": "apply",
 "nodes": [
  "hetzner"
 ],
 "note": "seq=132 SUPERSEDED -> noop. Original deep-crawl worker failed at runtime (PK assumption) and blocked the fail-closed pipe (cursor frozen at 131). seq-133 is the fixed v2 (exits 0). This noop advances the cursor past 132 so 133 applies and the pipe resumes. Unblock by Claude.",
 "steps": [
  {
   "type": "noop",
   "note": "supersede failed seq-132; advance cursor so seq-133 v2 deep-crawl applies"
  }
 ]
}