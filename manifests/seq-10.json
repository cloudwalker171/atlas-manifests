{
  "seq": 10,
  "mode": "apply",
  "note": "seq=10 NO-OP (EDGAR deferred to its own lane). The SEC EDGAR UA-403 was FIXED and the collector successfully landed ~3500+ real companies, but its long full-population submissions run held the deploy pipe open (~18min) and blocked the urgently-needed metrics bridge + enrichment scale-up. Per operator directive, advancing last_seq via no-op so seq-11 (metrics) and seq-12 (fleet scale) land now. Already-committed EDGAR businesses persist (idempotent on source_record). EDGAR to be re-published as a bounded/capped lane later.",
  "steps": [
    {
      "type": "noop",
      "note": "seq=10 intentionally no-op; EDGAR deferred so downstream seqs (metrics, scale) apply."
    }
  ]
}