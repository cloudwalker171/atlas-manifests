{
  "seq": 129,
  "mode": "apply",
  "note": "seq=129: SUPERSEDED to noop. Original 129 (czds status patch) failed in-pipe, but the patch was already applied directly on the box (czds.json is publishing: status=live, 16055 new .org rows). Replacing with a noop so the deploy lane advances to cursor 129 and future deploys flow.",
  "nodes": [
    "hetzner"
  ],
  "steps": [
    {
      "type": "noop",
      "note": "seq=129 superseded -> noop (czds fix already live; unblock the lane)."
    }
  ]
}