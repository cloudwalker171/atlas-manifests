{
 "seq": 134,
 "mode": "apply",
 "nodes": [
  "hetzner"
 ],
 "note": "seq=134 SUPERSEDED -> noop. The original diagnostic script could exit non-zero on error and wedge the fail-closed pipe (cursor frozen at 133). Noop so the applier advances to seq-135 (light v3 email engine) and the pipe flows. Diagnostics will be re-shipped as exit-0-safe.",
 "steps": [
  {
   "type": "noop",
   "note": "superseded->noop to keep the pipe draining (no failing run step)"
  }
 ]
}