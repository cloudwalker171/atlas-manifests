{
 "seq": 136,
 "mode": "apply",
 "nodes": [
  "hetzner"
 ],
 "note": "seq=136 SUPERSEDED -> noop. Same reason as 134 (diag could exit non-zero). Keep the pipe draining; re-ship diagnostics exit-0-safe later.",
 "steps": [
  {
   "type": "noop",
   "note": "superseded->noop to keep the pipe draining (no failing run step)"
  }
 ]
}