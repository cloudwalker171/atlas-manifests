{
  "seq": 20,
  "mode": "apply",
  "note": "seq=20 NO-OP: MX qualifier deferred. Its --selftest failed on the recurring business_ref-vs-business_id schema bug (field_provenance real PK is business_id/source_code, not business_ref/source), AND it qualifies the CT/SSL lane which is not yet inflowing. No-op to unblock the pipe so the Brain-glue fix + strategy items land. Redo MX (schema-corrected) after CT Channel-1 inflow is confirmed.",
  "steps": [
    {
      "type": "noop",
      "note": "seq=20 no-op; MX deferred (schema fix + CT-inflow needed first)"
    }
  ]
}