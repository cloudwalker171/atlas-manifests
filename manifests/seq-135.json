{
 "seq": 135,
 "mode": "apply",
 "nodes": [
  "hetzner"
 ],
 "note": "seq=135 SUPERSEDED -> noop. The v3 email worker file is already written on the box; running a crawl batch synchronously in the deploy step ties up the applier for minutes. Email crawling now runs ONLY via the seq-137 background scaler timer (bounded, non-blocking). Noop keeps the pipe fast.",
 "steps": [
  {
   "type": "noop",
   "note": "email work moved to background scaler (seq-137)"
  }
 ]
}