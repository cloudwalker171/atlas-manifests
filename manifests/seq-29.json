{
  "seq": 29,
  "mode": "apply",
  "note": "seq=29 NO-OP: cold-archive deferred (its apply failed and blocked seq-31 metrics-never-null = the dashboard fix). Unblock the data queue; re-deploy cold-archive schema-corrected later.",
  "steps": [
    {
      "type": "noop",
      "note": "seq=29 no-op; cold-archive deferred to unblock seq-31 metrics fix"
    }
  ]
}